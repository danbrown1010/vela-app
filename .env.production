# VITE_ENV_LABEL intentionally absent — DevRibbon is hidden in production builds.
# To show the ribbon in a preview/staging build, pass VITE_ENV_LABEL=PREVIEW
# as an environment variable when running `npm run build`.
